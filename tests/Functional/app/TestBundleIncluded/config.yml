imports:
    - { resource: ../default/config.yml }

framework:
    profiler:
        enabled: true
        collect: true

spiechu_symfony_commons:
    response_schema_validation:
        enabled: true
    api_versioning:
        enabled: true
        features:
          feature_a:
            since: 1.0
            until: 1.2
