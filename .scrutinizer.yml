build:
    environment:
        php:
            version: 7.1
    tests:
        override:
            -
                command: 'vendor/bin/simple-phpunit --coverage-clover=some-file'
                coverage:
                    file: 'some-file'
                    format: 'clover'
